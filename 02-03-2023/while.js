const nomesDePaises = ['Brasil', 'Argentina', 'Bolivia', 'Uruguai', 'Peru'];

let indice = 0;

while (indice < nomesDePaises.length) {//enquanto
    console.log(nomesDePaises[indice]);
    indice++;
}
